import React from "react";
import Layout from '../components/layout';

export default () => (
  <Layout>
    <h1>Gatsby Typography Plugin</h1>
    <p>
      This all looks better than if I designed it myself.
    </p>
    <p>
      You probably only want 2 or MAYBE 3 differents on a site.
    </p>
  </Layout>
);
